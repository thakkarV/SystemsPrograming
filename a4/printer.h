#ifndef PRINTER
#define PRINTER

#include <stdio.h>
#include <stdlib.h>

#include "datastructures.h"

void print_matrix(const matrix * matrix);
matrix * transpose(const matrix * matrix);

#endif
